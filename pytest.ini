[pytest]
pythonpath = src
addopts = --import-mode=importlib
markers =
    integration: tests that hit external systems
testpaths =
    tests/api
    tests/cli
    tests/adapters
    tests/domain
    tests/application
    tests/services
